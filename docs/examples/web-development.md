# 🌐 Web 開發案例

> 使用 OpenCode 進行 Web 開發的完整工作流程！

## 🎯 Web 開發概覽

OpenCode 是 Web 開發的強大助手，無論是前端框架、後端服務，還是全端應用，都能提供專業支援。

## 🚀 快速開始專案

### 🎯 **創建 React 專案**

```bash
# 啟動 OpenCode
opencode

# 創建新專案
> 請幫我創建一個 React + TypeScript 專案，
  使用 Vite 作為建置工具，
  包含 TailwindCSS 樣式框架
```

AI 將自動執行：
```
🔧 正在創建專案結構...
📦 安裝 React、TypeScript、Vite...
🎨 配置 TailwindCSS...
📝 創建基礎元件...
✅ 專案創建完成！

npm run dev 啟動開發伺服器
```

### 🎯 **創建 Next.js 專案**

```bash
> 請創建一個 Next.js 14 應用程式，
  使用 App Router，
  包含認證系統和資料庫整合
```

## 🛠️ 元件開發

### 🎯 **創建 UI 元件**

```bash
> 請創建一個可重用的 Button 元件，支援：
  - 不同尺寸 (sm, md, lg)
  - 不同樣式 (primary, secondary, outline)
  - 載入狀態
  - 禁用狀態
```

生成的元件：
```tsx
// components/Button.tsx
interface ButtonProps {
  size?: 'sm' | 'md' | 'lg';
  variant?: 'primary' | 'secondary' | 'outline';
  loading?: boolean;
  disabled?: boolean;
  children: React.ReactNode;
  onClick?: () => void;
}

export const Button: React.FC<ButtonProps> = ({
  size = 'md',
  variant = 'primary',
  loading = false,
  disabled = false,
  children,
  onClick
}) => {
  // 實作邏輯...
};
```

### 🎯 **創建表單元件**

```bash
> 請創建一個用戶註冊表單，包含：
  - 電子郵件驗證
  - 密碼強度檢查
  - 確認密碼比對
  - 即時錯誤提示
```

## 🔗 API 整合

### 🎯 **REST API 整合**

```bash
> 請幫我整合 /api/users 端點：
  - 實現 CRUD 操作
  - 添加錯誤處理
  - 實現分頁功能
  - 添加快取機制
```

### 🎯 **GraphQL 整合**

```bash
> 請設定 Apollo Client 並創建用戶查詢：
  - 配置 Apollo Provider
  - 創建 useUsers hook
  - 實現樂觀更新
```

## 🎨 樣式處理

### 🎯 **TailwindCSS 工作流**

```bash
> 請用 TailwindCSS 創建一個響應式導航欄：
  - 桌面顯示完整選單
  - 行動裝置顯示漢堡選單
  - 包含深色模式切換
```

### 🎯 **CSS Modules**

```bash
> 請將這個元件的樣式轉換為 CSS Modules，
  並確保沒有樣式衝突
```

## 📱 響應式設計

### 🎯 **響應式佈局**

```bash
> 請創建一個儀表板佈局：
  - 三欄佈局（桌面）
  - 兩欄佈局（平板）
  - 單欄佈局（手機）
  - 側邊欄可折疊
```

## ⚡ 效能優化

### 🎯 **程式碼分割**

```bash
> 請分析這個 React 應用的打包大小，
  並實施程式碼分割優化
```

### 🎯 **圖片優化**

```bash
> 請幫我優化所有圖片：
  - 使用 Next.js Image 元件
  - 實現懶載入
  - 添加適當的 placeholder
```

## 🧪 測試

### 🎯 **元件測試**

```bash
> 請為 Button 元件寫測試：
  - 測試所有 props 組合
  - 測試點擊事件
  - 測試無障礙性
```

### 🎯 **E2E 測試**

```bash
> 請使用 Playwright 寫登入流程的 E2E 測試
```

---

## 🚀 下一步

👉 **[繼續：API 開發 →](api-development.md)**

---

<div align="center">
  <p><strong>🌐 Web 開發高手！</strong></p>
</div>
